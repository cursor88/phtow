# 从想法到落地：用 AI 打造「草木有方」中药学习小程序

> 作者：开发者自述 | 项目名：草木有方 | 技术栈：TRAE AI + 前后端全栈

---

## 一、缘起：一次低成本的 AI 探索

事情是这样的，最近想用 AI 做一些感兴趣的小程序。一开始当然是想用最低成本，主要是想打通整个流程，看看免费的工具能不能给我带来点惊喜，能不能把我的想法给做出来。目前整个过程还算蛮流畅的——免费的东西还要什么自行车！

### 1.1 工具选择：从 TRAE IDE 到 TRAE Work

一开始我下载了 TRAE IDE，让 TRAE 生成了前后端项目，以及本地的 MySQL 服务。本来我是想自己把控整个代码，后面想着，先用 TRAE Work 试试吧。于是乎用自然语言生成了一个中药学习的 demo，整个过程还是挺有趣的。

**工具演进**：
- **初期**：TRAE IDE + 本地 MySQL（想自己把控代码）
- **后期**：TRAE Work + 本地 SQLite（快速开发，轻量数据库更适合）

### 1.2 意外惊喜：TRAE 创造力大赛

好巧不巧，打开掘金后，无意间看到了 TRAE 的创造力大赛——**报名就有一个月的速通奖励**！哈哈哈哈，白嫖一下试试水。于是乎，顺利通过了报名，拿到了一个月的速通奖励。

---

## 二、核心功能开发：踩坑与成长

### 2.1 功能一：药材拍照识别（最烧脑的功能）

#### 最初的想法
通过拍照识别出中药药材，然后把一些药材的信息展示出来。

#### 踩坑之路

**第一坑：本地检索数据不足**

一开始是本地学习，药材识别依靠本地检索，但数据不够，识别效果很差。

**第二坑：通用大模型识别效果差强人意**

于是我选择调用 AI 大模型，但通用型模型对中药材的识别效果并不好——很多药材外观相似，通用模型缺乏专业知识。

**第三坑：多模型识别成本高**

尝试了多个模型识别，取出最好的结果。但这需要多个模型的 API Key，让用户设置多个 API 太麻烦了。

#### 最终方案：混合模式

> **专用模型 + 本地检索 + 相似度推荐**

```
用户上传图片 → 专用模型识别 → 本地数据库检索 → 返回主结果 + 相似药材对比
```

虽然比不上专门训练过的专业模型，但让用户通过这个方式去学习药材，或是识别到其他相似药材做个对比，**学习目的完全够用**。

我们都知道，药材这个东西，有些药材外观还是挺接近的。除了外观，还要靠气味、质地等方式判断，单独通过照片有些药材还是无法准确识别。不过我的目的是通过这个方式去学习一些中药材的东西，所以目前这个功能我觉得够用。

> 💡 学到的道理：
> - 中药材的识别不能只靠外观，还要靠气味、质地等多方面判断
> - 单独通过照片有些药材确实无法准确识别
> - 作为学习工具，提供对比和参考比追求绝对准确更有价值

---

### 2.2 功能二：趣味问答检测学习效果

在实现了识别功能后，我想到了：**怎么检验用户是否真的学到了东西？**

于是加了随机生成的问答题功能：
- 选择题形式
- 涵盖药材的性味、归经、功效、主治等知识点
- 答题后显示正确答案和详细解析

我想这个功能也能一定程度上去检测用户是不是对药材达到一定认识。

> 💡 学到的道理：
> - 学习需要反馈机制，问答是很好的检测手段
> - 通过答题可以强化记忆，加深理解

---

### 2.3 功能三：每日打卡学习

刷到一个公众号文章，主题是"每日学习一个中药材"。我心想：**这个思路太棒了！**

于是实现了每日打卡功能：
- 每天推荐一个中药材
- 用户打卡记录学习进度
- 可以查看学习历史

> 💡 学到的道理：
> - 学习需要坚持，打卡机制可以培养习惯
> - 碎片化学习适合现代人的生活节奏

---

### 2.4 功能四：经方问诊（融合倪海厦 Skill）

刷到了倪海厦医师的 Skill，想着怎么融合进来，目前做了一个问诊的效果，不过也是需要接入 LLM，借助大模型优化输入输出的结果。

#### 实现过程

**第一阶段：本地知识库检索**

使用倪海厦经方理论知识库（伤寒论、金匮要略等 1143 条索引），通过关键词匹配进行辨证。

**第二阶段：接入 LLM 优化**

发现纯本地检索的结果不够智能，于是接入 LLM 优化输入输出：
- 症状描述的理解更准确
- 辨证分析更全面
- 处方建议更合理

**遇到的问题与解决**

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 所有消息都返回"需要先了解您的症状" | 消息分类顺序错误 | 调整分类优先级，症状报告优先于追问 |
| 回答出现 Markdown 符号（###、**） | 未做格式化处理 | 添加 formatResponse 方法去除符号 |
| 排版拥挤没有分行 | CSS 样式缺失 | 添加 `white-space: pre-line`，`\n` 替换为 `<br>` |
| 问诊记录点击后显示"会话已过期" | 会话未持久化 | 从历史记录恢复 session 到内存 |

> 💡 学到的道理：
> - 专业领域的知识需要专业知识库支撑
> - LLM 可以提升交互体验，但不能替代专业知识
> - 会话管理需要考虑持久化和恢复机制

---

### 2.5 功能五：体质测评与药材推荐

后来我又想到：**用户学习药材的目的是什么？很多人是想知道自己适合用什么药材来调理身体。**

于是设计了体质测评功能，从自身体测出发，到推荐药材搭配学习，形成一个完整的学习闭环。

#### 测评体系

采用中医九种体质分类法：
- **平和质**：阴阳气血调和，健康状态
- **气虚质**：元气不足，易疲劳
- **阳虚质**：阳气不足，畏寒怕冷
- **阴虚质**：阴液亏少，口干咽燥
- **痰湿质**：痰湿凝聚，体型肥胖
- **湿热质**：湿热内蕴，面垢油光
- **血瘀质**：血行不畅，肤色晦暗
- **气郁质**：气机郁滞，情绪低沉
- **特禀质**：先天禀赋异常，过敏体质

#### 测评流程

```
开始测评 → 答题（9题快速版 / 60题标准版）→ 分析得分 → 生成体质报告 → 推荐药材搭配
```

#### 核心功能

**测评结果展示**：
- 体质类型和描述
- 主要特征标签
- 饮食建议
- 运动建议
- **推荐药材（可点击查看详情）**

**推荐逻辑**：
```javascript
// 九种体质对应的推荐药材
const CONSTITUTION_RESULTS = {
  "平和": { name: "平和质", herbs: ["枸杞", "甘草", "茯苓"] },
  "气虚": { name: "气虚质", herbs: ["黄芪", "人参", "白术"] },
  "阳虚": { name: "阳虚质", herbs: ["生姜", "当归", "人参"] },
  "阴虚": { name: "阴虚质", herbs: ["枸杞", "丹参", "甘草"] },
  "痰湿": { name: "痰湿质", herbs: ["茯苓", "白术", "甘草"] },
  "湿热": { name: "湿热质", herbs: ["茯苓", "丹参", "甘草"] },
  "血瘀": { name: "血瘀质", herbs: ["丹参", "当归", "甘草"] },
  "气郁": { name: "气郁质", herbs: ["甘草", "茯苓", "枸杞"] },
  "特禀": { name: "特禀质", herbs: ["甘草", "黄芪", "白术"] }
};
```

**交互设计**：

测评结果中的推荐药材标签是可点击的，点击后直接跳转到药材详情页面，方便用户深入学习。

```javascript
// 推荐药材标签点击事件
result.herbs.map(function(h){
  var herb = HERB_DATA.find(function(herb){ return herb.name === h; });
  return '<span onclick="closeConstitutionTest();showHerbDetail('+herb.id+')">'+h+'</span>';
}).join('')
```

#### 学习闭环

```
体质测评 → 了解自身体质 → 获取推荐药材 → 学习药材知识 → 应用到日常养生
```

> 💡 学到的道理：
> - 学习需要明确的目标导向，知道"为什么学"比"学什么"更重要
> - 个性化推荐可以提高学习的针对性和实用性
> - 从了解自己出发，学习效果更好

---

## 三、技术架构与实现

### 3.1 项目结构

```
草木有方/
├── server/                    # 后端服务
│   ├── app.js                 # 服务入口
│   ├── routes/                # API 路由
│   │   ├── herb.js            # 药材相关接口
│   │   ├── identify.js        # 识别相关接口
│   │   ├── chat.js            # 问诊对话接口
│   │   ├── quiz.js            # 问答接口
│   │   ├── checkin.js         # 打卡接口
│   │   └── constitution.js    # 体质测评接口
│   ├── services/              # 业务逻辑层
│   │   ├── llmService.js      # LLM 服务（支持5种提供商）
│   │   ├── chatService.js     # 会话管理服务
│   │   └── nihaixiaService.js # 倪海厦知识库服务
│   └── data/                  # 数据层
│       ├── herbs.js           # 药材数据
│       ├── quizzes.js         # 问答数据
│       └── nihaixia/          # 倪海厦知识库
└── demo-online.html           # 前端单页应用（演示版）
```

### 3.2 核心技术亮点

**LLM 服务支持多种提供商**

```javascript
// 支持 OpenAI、智谱AI、月之暗面、通义千问、豆包
// 按优先级自动检测可用的 API Key
const providers = {
  openai: new OpenAIProvider(),
  zhipu: new ZhipuProvider(),
  moonshot: new MoonshotProvider(),
  qwen: new QwenProvider(),
  doubao: new DoubaoProvider()
};
```

**混合模式检索策略**

```javascript
// 本地知识库检索 + LLM 推理
async function search(query) {
  // 1. 本地关键词匹配
  const localResults = await localSearch(query);
  
  // 2. 如果本地结果不足，调用 LLM
  if (localResults.length < 3) {
    const llmResults = await llmService.search(query);
    return [...localResults, ...llmResults];
  }
  
  return localResults;
}
```

**会话恢复机制**

```javascript
// 从历史记录恢复会话
function getHistoryDetail(sessionId) {
  const session = this.sessions.get(sessionId);
  if (session) return session;
  
  // 从持久化存储恢复
  const record = this.chatHistory.find(h => h.sessionId === sessionId);
  if (record) {
    const restoredSession = { ...record, lastActivity: Date.now() };
    this.sessions.set(sessionId, restoredSession);
    return restoredSession;
  }
  
  return null;
}
```

**体质测评算法**

```javascript
// 根据答题得分判断体质类型
function constitutionSubmit() {
  var scores = {};
  constitutionAnswers.forEach(function(a){
    if (!scores[a.type]) scores[a.type] = 0;
    scores[a.type] += a.score;
  });
  
  var maxScore = -1, maxType = '平和';
  for (var type in scores) {
    if (scores[type] > maxScore) {
      maxScore = scores[type];
      maxType = type;
    }
  }
  
  var result = CONSTITUTION_RESULTS[maxType];
  // 生成测评报告...
}
```

---

## 四、快速开发的秘诀

### 4.1 AI 辅助开发的优势

**速度快**：几分钟就能把想法落地
```
自然语言描述需求 → TRAE 生成代码 → 调试优化 → 完成功能
```

**成本低**：免费工具 + 免费额度就能完成开发

**学习曲线平缓**：不需要从零开始，可以逐步深入

### 4.2 需要自己打磨的部分

**数据质量**：AI 生成的数据需要人工审核和补充

**用户体验**：界面设计、交互细节需要自己调整

**业务逻辑**：核心业务规则需要自己思考和优化

---

## 五、项目效果展示

### 5.1 首页界面

展示每日药材、快捷入口、学习进度等模块。

### 5.2 药材识别

- 拍照或上传图片
- 识别结果展示药材详细信息
- 显示相似药材供对比学习

### 5.3 经方问诊

- 聊天式交互界面
- 智能辨证分析
- 处方建议和调养指导

### 5.4 体质测评

- 9 题快速测评 / 60 题标准测评
- 九种体质分类
- 个性化饮食、运动、药材推荐
- 推荐药材可点击查看详情

### 5.5 学习模块

- 每日打卡
- 趣味问答
- 药食同源搭配

---

## 六、总结与展望

### 6.1 学到的东西

1. **AI 辅助开发不是替代开发者，而是提升效率**——整个过程其实很快，几分钟就能把想法落地
2. **快速原型验证的重要性**——先把想法落地，再逐步优化
3. **专业领域知识的价值**——中药材识别需要专业知识库支撑
4. **学习工具的设计思路**——提供对比和参考比追求绝对准确更重要
5. **从用户需求出发**——体质测评让用户从了解自己开始学习，效果更好
6. **学习闭环的重要性**——测评 → 推荐 → 学习 → 应用，形成完整的学习路径

### 6.2 后续计划

1. **完善药材数据**——增加更多药材的详细信息
2. **优化识别算法**——结合更多特征进行识别
3. **扩展问诊功能**——支持更复杂的辨证流程
4. **增强体质测评**——增加更多个性化推荐
5. **增加社区功能**——让用户可以分享学习心得

---

## 七、写在最后

> "整个过程其实很快，几分钟就能把想法落地。这确实是 AI 带来的很大便利性。当然这个需要自己去继续打磨，在提高实用性方面还需要靠自己去一步步调教。"

对于一个药材学习 demo，目前根据自己想法生成出来的效果还是令人满意的。后续就是把数据搞好，相信能帮到对中医感兴趣的用户。

**项目地址**：[草木有方 GitHub](https://github.com/your-repo)
**演示地址**：http://localhost:3000/demo-online.html

---

*本文记录了「草木有方」小程序从想法到落地的全过程，希望能给同样想用 AI 快速开发的朋友们一些参考和启发。*
